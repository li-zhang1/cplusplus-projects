#include <iostream>
#include <cmath>

int main(){
    double x = 3;
    double x1 = 3.14;
    double x2 = 3.99;
    double y = 4;
    double z;

    // z = std::max(x, y);
    // z = std::min(x, y);
    //z = pow(2, 4);
    //z = sqrt(9);
    //z = abs(-1);
    //z = round(x1);
    //z = ceil(x1);
    //z = floor(x2);
    std::cout << z << '\n';

    return 0;
}